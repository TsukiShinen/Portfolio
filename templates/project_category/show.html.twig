{% extends 'baseAdmin.html.twig' %}

{% block title %}ProjectCategory{% endblock %}

{% block body %}
    <h1>ProjectCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_project_category_index') }}">back to list</a>

    <a href="{{ path('app_project_category_edit', {'id': project_category.id}) }}">edit</a>

    {{ include('project_category/_delete_form.html.twig') }}
{% endblock %}
