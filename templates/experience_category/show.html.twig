{% extends 'baseAdmin.html.twig' %}

{% block title %}ExperienceCategory{% endblock %}

{% block body %}
    <h1>ExperienceCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ experience_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ experience_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_experience_category_index') }}">back to list</a>

    <a href="{{ path('app_experience_category_edit', {'id': experience_category.id}) }}">edit</a>

    {{ include('experience_category/_delete_form.html.twig') }}
{% endblock %}
